# QA Tester Run Log

Read this FIRST every run. Append your findings at the bottom.

## Format
Each entry: `[DATE] — RESULT — SUMMARY`

## Known Patterns
Track recurring failures here so you don't re-report them:

## Known Patterns

- **Unmerged branch pattern**: Tasks are regularly completed in local worktrees (ao/task-XXX) and marked "done" but never pushed to origin or merged to main. This has recurred every single run. TASK-010/018/021/026/035/047 were all created to address this. The fix (TASK-047) remains in backlog.
- **pnpm db:push not run after merges**: Every time schema-changing branches merge (TASK-022, TASK-031, TASK-033), the DB isn't updated — causing SqliteError crashes on dashboard/posts/calendar/analytics/campaigns. Pattern: merge → crash → TASK to run db:push.
- **AI gen always 500/401**: ANTHROPIC_API_KEY is set in .env but invalid — Anthropic API returns 401 authentication_error. As of run 10, raw 401 JSON is exposed directly to user in the UI.
- **Empty "done" branches**: Tasks are marked "done" but the ao/task-XXX branch contains ZERO unique commits (branch just points to a recent main commit). Seen with TASK-045/046/047/051/052/053/054 in runs 10-11. The worktrees exist but no work was committed. Different from the "unmerged branch" pattern — here no work was done at all. Now 11 consecutive runs with this pattern.
- **Dev server environment churn**: PostPilot dev server startup is not stable across runs. Previously CondoHub occupied :3000 (runs 1-15), PostPilot ran on :3001. Run 16: launchapp-nextjs occupied :3000/:3001/:3002, PostPilot not running. Run 17: environment settled — :3000=CondoHub, :3001=PostPilot (PID 65539), :3002=Invoicer. BUT PostPilot server hangs after BUG-015 crash on initial request — accepts TCP but no HTTP responses.
- **BUG-015 causes server hang**: When PostPilot dev server starts and receives first request to `/`, it logs the buttonVariants() error and then stops responding to ALL HTTP requests (TCP connections still accepted). This means even auth/api routes are unreachable after the server hits BUG-015. Fixing BUG-015 is now doubly critical — it not only breaks the landing page but renders the entire dev server non-functional.
- **Ghost-done BUG-015 fix (21+ times)**: TASK-046 through TASK-090 (21+ tasks) all marked "done" for deleting button.tsx. button.tsx still exists on main (only ever had commit 63cd453). No deletion commit has EVER been made. button.ts (the correct barrel, no "use client") exists alongside button.tsx. TASK-091 also ghost-done for accounts pages (same pattern — zero unique commits).

## Log

[2026-03-28 run 9] — FAIL — App state identical to run 8. No new commits to main. `/` 500 (BUG-015), /dashboard/posts/calendar/analytics 500 (BUG-016), /campaigns 500 (BUG-017), /accounts 404 (BUG-007). /posts/new + /settings + auth PASS. No new tasks created. Unmerged branches: ao/task-008/037/039/041. TASK-045/046/047 still in backlog.
[2026-03-29 run 10] — FAIL — TASK-050 (bulk actions) merged since run 9. All known bugs still present: `/` 500 (BUG-015), /dashboard/posts/calendar/analytics 500 (BUG-016), /campaigns 500 (BUG-017), /accounts 404 (BUG-007). Auth + /posts/new + /settings + /campaigns/new PASS. Bulk actions code merged but untestable (dashboard crashes). KEY FINDING: TASK-045/046/047/051 all marked "done" but their branches have ZERO unique commits — tasks were created, worktrees exist, but no code was ever committed. ao/task-008 still has the accounts fix (1 commit ahead of main) but has never been merged despite 8+ tasks created to merge it. No new tasks created.
[2026-03-29 run 11] — FAIL — No new code merged since run 10. TASK-052/053/054 marked done but all have 0 commits ahead of main. BUG-015/016/017/007/012 all still open. NEW FINDING: DB verified to only contain account/post/session/user/verification tables — settings/brandVoice/campaign tables missing, recycleCount/noRecycle missing from post table. Settings form permanently disabled (server action silently fails). pnpm db:push has NEVER been run since run 7. No new tasks created (all failures are known).
[2026-03-29 run 12] — FAIL — No new code merged since run 11. App state identical. BUG-015/016/017/007/018 all still open. Auth login works (POST /api/auth/sign-in/email 200) but dashboard crashes immediately (SqliteError: recycleCount). Logout click timed out (flaky). /posts/new + /settings (loads, fields disabled) + /campaigns/new PASS. No new tasks created.
[2026-03-28 run 13] — FAIL — No new code merged since run 12. App state identical. BUG-015/016/017/007/012/018 all persist. `/` 500 (BUG-015), /dashboard 500 (BUG-016), /posts 500, /calendar 500, /analytics 500, /campaigns 500 (BUG-017), /accounts 404 (BUG-007). /posts/new + /settings + /campaigns/new + /signup + /login PASS. AI gen 500 (BUG-012). Logout click timed out (flaky, 13th consecutive). Browser-based login redirects to crashing dashboard. Auth API (POST /api/auth/sign-in/email) still returns 200. No new tasks created — all failures are known bugs.
[2026-03-28 run 14] — FAIL — No new code merged since run 13. App state identical. BUG-015/016/017/007/012/018 all persist. `/` 500 (SqliteError recycleCount on landing), /dashboard/posts/calendar/analytics/campaigns all 500, /accounts 404. /posts/new + /settings (fields disabled) + /campaigns/new + /signup + /login PASS. AI gen 500 — raw Anthropic 401 JSON still showing in prompt area (BUG-012). Logout click timed out (flaky, 14th consecutive). Login API 200 confirmed via browser flow. No new tasks created — all failures are known bugs.
[2026-03-28 run 15] — FAIL — No new code merged since run 14. App state identical. BUG-015/016/017/007/012/018 all persist. `/` 500 (SqliteError recycleCount), /dashboard/posts/calendar/analytics/campaigns all 500, /accounts 404. /posts/new + /settings (fields disabled) + /campaigns/new + /signup + /login PASS. AI gen 500 (/api/posts/generate). Logout click timed out (flaky, 15th consecutive). Login confirmed via browser flow (redirects to /dashboard 500). No new tasks created — all failures are known bugs.
[2026-03-29 run 16] — BLOCKED — PostPilot dev server NOT running. Previously ran on :3001 (CondoHub had :3000). Now environment completely changed: :3000/:3001/:3002 all occupied by launchapp-nextjs instances. :3000 shows ZodError (DATABASE_URL missing) in Next.js error overlay. No PostPilot next-server process found in process list. 0/6 tests runnable. No new tasks created — ops issue (server not started).
[2026-03-29 run 17] — FAIL — PostPilot dev server running on :3001 (PID 65539, next-server v16.2.1). Environment: :3000=CondoHub, :3001=PostPilot, :3002=Invoicer. Server starts, logs BUG-015 buttonVariants() error at 00:02:36 (confirmed in .next/dev/logs/next-development.log), then hangs — accepts TCP connections but never sends HTTP responses. All 6 tests untestable. No new code merged since run 16 (only memory/planner/product-owner commits on main). NEW FINDING: BUG-015 crash causes complete server HTTP hang, not just 500 on landing page. No new tasks created — all failures are known bugs.
[2026-03-29 run 18] — FAIL — Same PID 65539 on :3001, same HTTP hang. No new code merged since run 17 (only memory/planner/product-owner/reconciler commits). TASK-082/083 ghost-done since run 17: button.tsx still exists (git log shows only 63cd453). TASK-086 created as 18th attempt. 0/6 tests runnable. No new tasks created — all failures are known bugs.
[2026-03-29 run 19] — FAIL — Same PID 65539 on :3001, same HTTP hang. No new code merged since run 18 (only memory/planner/product-owner commits). TASK-086/088/090 all ghost-done (21st attempt total). TASK-091 ghost-done for accounts pages. button.tsx still present (only ever 63cd453). Port 3000=CondoHub (PID 11630), port 3002=Invoicer (PID 14364). 0/6 tests runnable. No new tasks created — all failures are known bugs.
[2026-03-29 run 20] — BLOCKED — PostPilot dev server completely gone (PID 65539 no longer exists). No new code merged since run 19 (only memory/planner/product-owner commits on main). button.tsx still present (only ever 63cd453). Environment: :3000=CondoHub (PID 96743), :3002=Invoicer (PID 29494), PostPilot=not running. 0/6 tests runnable. No new tasks created — all failures are known bugs and server is not started.
[2026-03-29 run 22] — PARTIAL PASS — No new code merged since run 21 (only memory/planner/reconciler commits). Server started on :3001 (PID 53780, nvm node v22). Environment: all ports idle at start. Server responds normally (no HTTP hang). / 500 (BUG-015 button.tsx still present — TASK-107 now in active triage). /accounts 404 (BUG-007). /api/posts/generate 503 (BUG-012, ANTHROPIC_API_KEY not configured). All other routes 200 when authenticated. Auth PASS (login 200, session established). No new tasks created — all failures are known bugs.
[2026-03-29 run 23] — PARTIAL PASS — No new code merged since run 22 (only memory/planner/reconciler commits). Server on :3001 (PID 53838). / 500 (BUG-015). /accounts 404 (BUG-007). AI gen 503 (BUG-012). All other routes 200. Auth PASS. NEW VERIFIED: Save Draft fully tested end-to-end for first time — post saved to /posts/[id], appeared in dashboard with Draft status, edit page loaded. Logout timeout (flaky, 23rd consecutive run). asChild DOM warning (BUG-019) on all authenticated routes. No new tasks created — all failures are known bugs.
[2026-03-29 run 24] — PARTIAL PASS — No new code merged since run 23 (only memory/planner commits). Server started manually on :3001 (PID 44004). / 500 (BUG-015). /accounts 404 (BUG-007). AI gen 503 (BUG-012). All other routes 200. Auth PASS. Platform select + manual content entry PASS. Logout timeout (flaky, 24th consecutive run). Health score ~72/100. No new tasks created — all failures are known bugs.
[2026-03-29 run 25] — PARTIAL PASS — No new code merged since run 24 (only memory/planner commits). Server already running on :3001. / 500 (BUG-015). /accounts 404 (BUG-007). AI gen 503 (BUG-012). All other routes 200. Auth PASS (login → /dashboard, browser confirmed). Save Draft PASS (created post /posts/s1xqtc5dlbmnc98z4i, 3 drafts visible in dashboard). Logout timeout (flaky, 25th consecutive run). Console errors only known bugs (BUG-019 asChild, BUG-013 error=false). No new tasks created — all failures are known bugs.
[2026-03-29 run 26] — PARTIAL PASS — No new code merged since run 25 (only memory/planner commits). Server on :3001 (PID 53838). / 500 (BUG-015). /accounts 404 (BUG-007). AI gen 503 (BUG-012). All other 10 routes 200 when authenticated. Auth PASS (POST /api/auth/sign-in/email 200, session token obtained). No new bugs found. No new tasks created — all failures are known bugs.
[2026-03-29 run 27] — PARTIAL PASS — No new code merged since run 26 (only memory/planner/reconciler commits). Server already running on :3001 (PID 53893). / 500 (BUG-015). /accounts 404 (BUG-007). AI gen 503 (BUG-012). All other routes 200. Auth PASS (browser login → /dashboard, 4 drafts visible). Save Draft PASS (post /posts/egres979jrmncbbnzs created, appeared in dashboard). Logout timeout (flaky, 26th+ consecutive run). Console errors: BUG-019 asChild, BUG-013 error=false. No new tasks created — all failures are known bugs.
[2026-03-29 run 28] — PARTIAL PASS — No new code merged to main since run 27 (only memory/planner commits). Server on :3001 (PID 53893). / 500 (BUG-015). /accounts 404 (BUG-007). AI gen 503 (BUG-012). All other routes 200. Auth PASS (browser login → /dashboard). Save Draft PASS (post /posts/nx1phhzpjqbmncdr54a created, appeared in dashboard). Logout timeout (flaky, 28th consecutive). KEY FINDING: TASK-107 branch (remotes/origin/ao/task-107) has REAL commit e16651d deleting button.tsx — first genuine BUG-015 fix attempt in 28 runs. Still not merged to main. No new tasks created — all failures are known bugs.
[2026-03-29 run 29] — PARTIAL PASS — No new code merged since run 28 (only memory/planner commits). New server started on :3001 (node v22, fresh start). / HTTP 500 (BUG-015). /accounts 404 (BUG-007). AI gen 503 (BUG-012). All other routes 200. Auth PASS (browser session active, dashboard loads with 5 drafts). Save Draft PASS (post /posts/tj63tzksovhmncejxrr created). Logout timeout (flaky, 29th consecutive). TASK-107 still in "ready" status — fix commit e16651d exists on origin/ao/task-107 but not merged. No new tasks created — all failures are known bugs.
